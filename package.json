{
  "name": "onshape-to-shaper",
  "version": "1.0.0",
  "description": "Extract from Onshape CAD to Shaper Origin CNC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "selection-server": "npm run build && node dist/cli.js",
    "serve-client": "npx http-server client -p 8080 --cors -o",
    "extract-svg": "npm run build && node dist/svg-extractor.js",
    "start": "npm run selection-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hovissimo/onshape-to-shaper.git"
  },
  "author": "Hovis Biddle",
  "license": "Unlicense",
  "bugs": {
    "url": "https://github.com/hovissimo/onshape-to-shaper/issues"
  },
  "homepage": "https://github.com/hovissimo/onshape-to-shaper#readme",
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "http-server": "^14.1.1",
    "typescript": "^5.3.0"
  }
}
